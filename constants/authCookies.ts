/**
 * AUTH COOKIE CONFIGURATION
 *
 * Constants for authentication cookie settings.
 * Used when setting cookies programmatically for auth.
 *
 * TODO: Update these values to match your application's cookie settings
 */

// Domain where auth cookies are valid (e.g., ".example.com")
export const AUTH_COOKIE_DOMAIN = ".example.com";

// Path restriction for auth cookies
export const AUTH_COOKIE_PATH = "/api";
